# HelixHive Phase 2 – Production Requirements
# =============================================================================
# All versions have been tested for compatibility (as of February 2026)
# =============================================================================

# Core dependencies – fundamental libraries used throughout the system
# -----------------------------------------------------------------------------
numpy==1.24.3                 # Leech vector operations, E8 encoding, RHC
pyyaml==6.0.2                  # Genome and config file loading
requests==2.32.3                # HTTP client (fallback for aiohttp)
aiohttp==3.13.3                 # Async HTTP client for LLM router and GitHub API [citation:5]
pyjwt==2.11.0                   # JWT generation for GitHub App authentication [citation:6]
pynacl==1.6.2                   # Libsodium bindings for GitHub secret encryption [citation:7]
tenacity==9.1.2                 # Retry logic with exponential backoff [citation:8]
scikit-learn==1.6.0             # DBSCAN clustering for faction detection [citation:9]

# OpenTelemetry – observability (metrics, tracing, logging)
# -----------------------------------------------------------------------------
opentelemetry-api==1.29.0       # Core API [citation:10]
opentelemetry-sdk==1.29.0       # SDK implementation [citation:10]
opentelemetry-exporter-prometheus==1.29.0  # Prometheus metrics exporter

# Google Cloud – optional for Gemini/Vertex AI integration
# -----------------------------------------------------------------------------
google-genai==1.5.0             # NEW unified Google GenAI library (replaces google-cloud-aiplatform) [citation:4]
google-cloud-secret-manager==2.22.0  # Secret management (if using GCP)
google-cloud-storage==3.1.0      # Cloud Storage for assets (if using GCP)

# LLM Provider SDKs – OpenAI‑compatible clients
# -----------------------------------------------------------------------------
groq==0.31.0                     # Groq API client [citation:2]
openrouter==0.6.0                 # OpenRouter API client (supports 300+ models) [citation:3]

# Testing – only needed for development
# -----------------------------------------------------------------------------
pytest==8.3.4                     # Test framework
pytest-cov==6.0.0                 # Coverage reporting
pytest-asyncio==0.25.3             # Async test support
hypothesis==6.119.0                # Property‑based testing

# -----------------------------------------------------------------------------
# Notes on version choices:
#
# - Python 3.10+ is required (3.10 is LTS, supported through 2026) [citation:1]
# - All async code uses aiohttp 3.13+ with asyncio [citation:5]
# - PyJWT 2.11.0 includes latest security fixes [citation:6]
# - PyNaCl 1.6.2 includes CVE fixes from libsodium 1.0.20 [citation:7]
# - scikit-learn 1.6+ is recommended by SPEC 0 [citation:9]
# - OpenTelemetry Python requires Python 3.9+ [citation:10]
# - groq SDK supports Python 3.8+ [citation:2]
# - openrouter SDK requires Python 3.9+ [citation:3]
# - google-genai is the new unified library (replaces separate Vertex/Gemini libs) [citation:4]
# -----------------------------------------------------------------------------
