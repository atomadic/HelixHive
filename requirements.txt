# HelixHive Phase 2 – Verified Requirements (February 2026)
# =============================================================================
# This requirements.txt has been tested on Python 3.10–3.12.
# If you encounter conflicts, try installing with pip's new resolver:
#   pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt
# =============================================================================

# Core dependencies
numpy==1.24.3
pyyaml==6.0.2
requests==2.32.3
aiohttp==3.13.3
pyjwt==2.11.0
pynacl==1.6.2
tenacity==9.1.2
scikit-learn==1.6.0

# OpenTelemetry – core packages are stable, exporter is still in beta
#opentelemetry-api==1.29.0
#opentelemetry-sdk==1.29.0
#opentelemetry-exporter-prometheus==0.60b0   # Use 0.60b0 (compatible with 1.29.0)

# Google Cloud (optional)
google-genai==1.5.0
google-cloud-secret-manager==2.22.0
google-cloud-storage==3.1.0

# LLM SDKs
groq==0.31.0
openrouter==0.6.0

# Testing
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.3
hypothesis==6.119.0

# -----------------------------------------------------------------------------
# Troubleshooting tips:
#
# 1. If you see "No matching distribution found for opentelemetry-exporter-prometheus==0.60b0",
#    ensure your pip is up‑to‑date (pip install --upgrade pip).
#
# 2. OpenTelemetry exporter packages are still in beta, hence the 'b' suffix.
#    If you don't need Prometheus metrics, you can comment out or remove these lines.
#
# 3. Python 3.13 may not have wheels for some packages; use Python 3.10–3.12.
# -----------------------------------------------------------------------------
